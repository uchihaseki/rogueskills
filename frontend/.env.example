# Relative URL (recommended for local development with the Vite /api proxy)
VITE_API_BASE_URL=/api

# For separately hosted frontend and backend deployments, use the full API URL:
# VITE_API_BASE_URL=http://127.0.0.1:5173
